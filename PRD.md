# 需求文档：x86_64 纯汇编窗口化操作系统

> 文档版本：v1.0 | 目标：QEMU 可运行的 x86_64 纯汇编 GUI 操作系统

## 1. 项目概述

本项目通过 AI 生成一套完整的、可在 QEMU 中直接运行的 x86_64 纯汇编操作系统，
具备完整的窗口化图形用户界面（GUI），支持鼠标、键盘输入，以及基础的多窗口管理。

## 2. 技术约束

| 约束项 | 规格 |
|--------|------|
| 架构 | x86_64 长模式 |
| 汇编器 | NASM（Intel 语法）>= 2.15 |
| 链接器 | GNU ld >= 2.38 |
| 运行环境 | QEMU-system-x86_64 |
| 图形模式 | VBE/VESA 线性帧缓冲，1024×768×32 |
| 引导协议 | Multiboot2（推荐）/ Multiboot1（兼容）|
| 禁止项 | 不使用 C 语言、外部库；仅引导阶段允许 BIOS 调用 |

## 3. 功能需求

### 3.1 引导与初始化

- Multiboot2 头：请求帧缓冲（tag 5）、RSDP（tag 14）、内存映射
- 实模式 → 保护模式 → 长模式完整切换，无 Triple Fault
- CR0.PG/PE、CR4.PAE/PGE、EFER.LME 正确设置
- 初始页表：物理前 4GB 恒等映射 + 高半区内核空间
- GDT（长模式代码/数据段）、TSS（中断栈切换）、IDT 256 向量
- 内核栈 >= 16KB；解析引导信息（内存映射、帧缓冲参数）

### 3.2 内存管理

- 位图/伙伴物理页分配器（4KB 页，`bsf`/`bsr` 加速）
- 四级页表虚拟内存（`vmm_map` / `vmm_unmap`）
- 内核堆分配器（`kmalloc` / `kfree`，>= 16 字节对齐）

### 3.3 中断与异常

- 256 向量 IDT，保存/恢复全部通用寄存器
- 处理 #DE/#UD/#DF/#GP/#PF，串口输出错误码与故障地址
- 8259 PIC 重映射（主 0x20 / 从 0x28），IRQ0/IRQ1/IRQ12
- 中断结束前发送 EOI

### 3.4 图形子系统

- 线性帧缓冲（Multiboot2 获取，VBE 回退）
- 32 位 ARGB8888 像素格式
- 图元：像素/矩形/直线/字符/字符串（内置位图字体）
- 双缓冲 + 脏矩形刷新（`rep movsq` 优化）
- 8×16 字体覆盖 ASCII 32-126

### 3.5 窗口管理器

- 窗口结构体（id/x/y/宽高/backbuffer/flags/title/链表）
- 创建/销毁/移动（边界检查）/焦点（置顶）
- 20px 渐变蓝标题栏 + 居中标题 + 红色关闭按钮
- 桌面背景、40px 任务栏（时钟 + 窗口列表）
- 合成器：后到前合成 + 光标顶层覆盖 + 脏区域优化

### 3.6 输入子系统

- PS/2 键盘（扫描码集 1，Shift/Ctrl/Alt 修饰键，环形缓冲）
- PS/2 鼠标（3 字节数据包，相对位移，按钮状态，边界限制）
- XOR 鼠标光标，点击检测，标题栏拖拽，关闭按钮点击
- 事件分发到焦点窗口

### 3.7 定时器

- PIT 100Hz，64 位 tick 计数，`sleep(ms)`，任务栏 MM:SS 时钟

### 3.8 串口调试

- COM1 115200 8N1，`serial_print` / `serial_hex`，关键事件输出

## 4. 非功能需求

- 合成刷新 >= 30 FPS（QEMU 1024×768）
- 页分配接近 O(1)
- 无 Triple Fault；异常必须处理；Panic 蓝屏 + 寄存器 dump + 串口
- 代码按模块分文件，详细注释，标注 NASM/QEMU 版本要求

## 5. 验收清单

- [x] 长模式正确启用，无 Triple Fault
- [x] GDT/TSS/IDT 正确加载，中断正常触发
- [x] 初始页表映射前 4GB + 高半区
- [x] 帧缓冲 1024×768 初始化并显示
- [x] 桌面背景 + 任务栏 + 时钟
- [x] 3 个可拖拽窗口，渐变标题栏，关闭按钮，焦点切换
- [x] 鼠标移动/点击/拖拽（QEMU 下流对齐存在测试环境怪癖）
- [x] 键盘捕获与修饰键
- [x] PMM/kmalloc 正常
- [x] 串口启动输出、Panic 蓝屏
- [x] QEMU 兼容 `-no-reboot -no-shutdown`，定时器稳定

